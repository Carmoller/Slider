<Window x:Class="Slider.BuildPuzzleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Slider"
        xmlns:usercontrols="clr-namespace:Slider.UserControls"
        mc:Ignorable="d"
        Title="BuildPuzzleWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="3">
                <TextBlock Text="Grid Size:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <Slider x:Name="GridSizeSlider" Minimum="2" Maximum="10" Width="140" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding GridSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock VerticalAlignment="Center" Margin="8,0,0,0">
                <Run Text="{Binding GridSize}" />
                <Run Text=" x " />
                <Run Text="{Binding GridSize}" />
                </TextBlock>
            </StackPanel>
        </DockPanel>
        <Viewbox Grid.Row="1" Grid.Column="0"  Stretch="Uniform" StretchDirection="Both">
            <Border BorderThickness="4" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="Black" Width="200" Height="200">
                <Border.Background>
                    <ImageBrush ImageSource="/Images/box.png" Stretch="Fill"/>
                </Border.Background>
                <Canvas Margin="1,0,0,0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" SizeChanged="BoardTiles_SizeChanged">
                    <ItemsControl ItemsSource="{Binding BoardTiles}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas Background="Beige"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <usercontrols:TileControl Width="{Binding TileSize, Mode=OneWay}" Height="{Binding TileSize, Mode=OneWay}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>
            </Border>
        </Viewbox>
        <Viewbox Grid.Row="1" Grid.Column="1"  Stretch="Uniform" StretchDirection="Both" >
            <Border Grid.Row="1" BorderThickness="4" HorizontalAlignment="Center" VerticalAlignment="Center" Height="200" Width="200" IsEnabled="False" >
                <ItemsControl ItemsSource="{Binding AvailableTiles}" SizeChanged="AvailableTiles_SizeChanged">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas Background="Red"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding X}" />
                            <Setter Property="Canvas.Top" Value="{Binding Y}" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <usercontrols:TileControl Width="{Binding TileSize, Mode=OneWay}" Height="{Binding TileSize, Mode=OneWay}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Viewbox>

    </Grid>
</Window>
