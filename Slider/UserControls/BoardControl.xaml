<UserControl x:Name="Root" x:Class="Slider.UserControls.BoardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Slider.UserControls"
             xmlns:viewmodels="clr-namespace:Slider.ViewModels"
             xmlns:usercontrols="clr-namespace:Slider.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MouseLeftButtonDown="Root_MouseLeftButtonDown" MouseMove="Root_MouseMove">
        <Viewbox Stretch="Uniform" StretchDirection="Both">
        <Border BorderThickness="4" HorizontalAlignment="Center" VerticalAlignment="Center" Height="200" Width="200" Background="Transparent">
            <ItemsControl x:Name="ItemsControlBoard" ItemsSource="{Binding Path=ItemsSource, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" SizeChanged="Tiles_SizeChanged" Background="Transparent">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="Red"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <usercontrols:TileControl Width="{Binding TileSize, Mode=OneWay}" Height="{Binding TileSize, Mode=OneWay}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Viewbox>
</UserControl>
