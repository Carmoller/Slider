# Copilot Instructions

## Project Guidelines
- User prefers explicit type declarations instead of 'var'. When instantiating objects, use the new() syntax (e.g., 'CornerPattern cornerPattern = new(3)' instead of 'var cornerPattern = new CornerPattern(3)'). Apply this consistently across C# code generation.